# 投票系统源码下载指南

## 方式一：通过项目页面下载

如果你在Coze云端环境，可以直接在项目页面下载源码：
1. 进入项目页面
2. 找到"导出"或"下载源码"按钮
3. 下载完整项目压缩包

## 方式二：文件列表

以下是项目的核心文件结构，你可以按照这个结构创建文件：

```
vote-system/
├── package.json                 # 项目依赖配置
├── tsconfig.json                # TypeScript配置
├── next.config.ts               # Next.js配置
├── tailwind.config.ts           # Tailwind CSS配置
├── postcss.config.mjs           # PostCSS配置
├── components.json              # shadcn/ui配置
├── .env                         # 环境变量（需要自己配置）
├── .gitignore                   # Git忽略文件
│
├── public/                      # 静态资源
│
├── scripts/
│   └── init-users.ts            # 初始化用户脚本
│
├── src/
│   ├── app/
│   │   ├── globals.css          # 全局样式
│   │   ├── layout.tsx           # 根布局
│   │   ├── page.tsx             # 首页
│   │   ├── robots.ts            # SEO配置
│   │   │
│   │   ├── admin/               # 后台管理页面
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx
│   │   │   ├── login/page.tsx
│   │   │   ├── votes/page.tsx
│   │   │   ├── users/page.tsx
│   │   │   ├── records/page.tsx
│   │   │   ├── records/[id]/page.tsx
│   │   │   └── links/page.tsx
│   │   │
│   │   ├── vote/[id]/page.tsx   # 投票页面
│   │   │
│   │   └── api/                 # API接口
│   │       ├── auth/
│   │       │   ├── login/route.ts
│   │       │   ├── logout/route.ts
│   │       │   └── me/route.ts
│   │       ├── votes/
│   │       │   ├── route.ts
│   │       │   └── [id]/
│   │       │       ├── route.ts
│   │       │       ├── candidates/route.ts
│   │       │       ├── records/route.ts
│   │       │       └── vote/route.ts
│   │       ├── candidates/[candidateId]/route.ts
│   │       ├── users/
│   │       │   ├── route.ts
│   │       │   └── [id]/route.ts
│   │       ├── agent-links/route.ts
│   │       ├── track-click/route.ts
│   │       └── upload/route.ts
│   │
│   ├── components/ui/           # UI组件库
│   │
│   ├── hooks/
│   │   └── use-mobile.ts
│   │
│   ├── lib/
│   │   ├── utils.ts
│   │   └── auth.ts
│   │
│   ├── server.ts
│   │
│   └── storage/database/
│       ├── supabase-client.ts
│       └── shared/
│           ├── schema.ts
│           └── relations.ts
│
└── 宝塔部署教程.md              # 部署教程
```

## 方式三：手动复制文件

如果你无法下载，可以手动复制以下核心文件的内容。
